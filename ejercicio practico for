#include <iostream>
using namespace std;

int main() {
    int i, n, suma = 0;

    cout << "Ingrese valor de n: ";
    cin >> n;

    for (i = 1; i <= n; i++) {
        if (i % 2 == 0)
            suma = suma + i;
    }

    cout << "La suma es igual a " << suma;
    return 0;
}
